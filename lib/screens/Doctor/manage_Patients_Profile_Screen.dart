import 'package:flutter/material.dart';

class ManagePatientsProfile extends StatelessWidget {
  const ManagePatientsProfile({super.key});
  static const routeName = '/manage-patients-profile';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Hello'),
    );
  }
}
